/* Versioned, public ITCC45 structured-practice content. */
const BSITOOPPractice = (() => {
  const CONTENT_VERSION = '2026.08';
  const topics = Object.freeze([
    { id: 'classes', number: 1, title: 'Classes', activity: 'itcc45-classes-blueprint' },
    { id: 'objects', number: 2, title: 'Objects', activity: 'itcc45-object-state' },
    { id: 'encapsulation', number: 3, title: 'Encapsulation', activity: 'itcc45-encapsulation-property' },
    { id: 'inheritance', number: 4, title: 'Inheritance', activity: 'itcc45-inheritance-lookup' },
    { id: 'abstraction', number: 5, title: 'Class Abstraction', activity: 'itcc45-abstraction-contract' },
    { id: 'polymorphism', number: 6, title: 'Polymorphism', activity: 'itcc45-polymorphic-dispatch' },
  ]);

  const challenges = Object.freeze([
    { id: 'classes-predict', topic: 'classes', kind: 'Predict state', title: 'What belongs to the class?', code: 'class Student:\n    school = "CMU"\n\nprint(Student.school)', prompt: 'What does this display?', choices: ['CMU', 'Student', 'Nothing, because no object exists'], answer: 0, explanation: 'school is a class attribute, so it exists on Student as soon as the class statement finishes.' },
    { id: 'classes-diagnose', topic: 'classes', kind: 'Diagnose', title: 'The missing receiver', code: 'class Student:\n    def describe():\n        return self.name', prompt: 'What must change first?', choices: ['Add self as the method parameter', 'Replace self with Student()', 'Remove the return statement'], answer: 0, explanation: 'An instance method receives the current object through its first parameter, conventionally named self.' },
    { id: 'classes-complete', topic: 'classes', kind: 'Complete code', title: 'Initialize an instance', code: 'class Student:\n    def __init__(self, name):\n        _____', prompt: 'Which line stores the name on each object?', choices: ['self.name = name', 'Student = name', 'name.self = Student'], answer: 0, explanation: 'Assigning to self.name creates or updates the attribute on the current instance.' },

    { id: 'objects-predict', topic: 'objects', kind: 'Predict state', title: 'Separate library records', code: 'first = LibraryBook("Clean Code")\nsecond = LibraryBook("Python 101")\nfirst.borrower = "Ana"\nprint(second.borrower)', prompt: 'What is printed?', choices: ['None', 'Ana', 'Clean Code'], answer: 0, explanation: 'first and second refer to different book objects. Checking out first does not change second.' },
    { id: 'objects-diagnose', topic: 'objects', kind: 'Diagnose', title: 'Alias or copy?', code: 'first = LibraryBook("Clean Code")\nchecked_out = first\nchecked_out.borrower = "Ana"', prompt: 'What is now true?', choices: ['first.borrower is also "Ana"', 'first is unchanged because assignment copies objects', 'A second LibraryBook was created'], answer: 0, explanation: 'Assignment copies the reference, not the object. Both names point to the same library record.' },
    { id: 'objects-complete', topic: 'objects', kind: 'Complete code', title: 'Call an object method', code: 'book = LibraryBook("Clean Code")\n_____', prompt: 'Which line calls summary() with book as self?', choices: ['book.summary()', 'LibraryBook.summary()', 'summary(LibraryBook)'], answer: 0, explanation: 'Calling through the object binds book to the method’s self parameter.' },

    { id: 'encapsulation-predict', topic: 'encapsulation', kind: 'Predict state', title: 'Preserve the invariant', code: 'record = GradeRecord(88)\ntry:\n    record.score = 120\nexcept ValueError:\n    pass\nprint(record.score)', prompt: 'What does the property still return?', choices: ['88', '120', 'None'], answer: 0, explanation: 'The setter rejects 120 before changing _score, so the valid state remains 88.' },
    { id: 'encapsulation-diagnose', topic: 'encapsulation', kind: 'Diagnose', title: 'Python privacy', code: 'record._score = 40', prompt: 'Which explanation is accurate?', choices: ['The underscore signals internal use but does not block access', 'Python refuses to run any underscore access', '_score is automatically encrypted'], answer: 0, explanation: 'A leading underscore is a convention. Double underscores trigger name-mangling, but neither is Java-style enforced privacy.' },
    { id: 'encapsulation-complete', topic: 'encapsulation', kind: 'Complete code', title: 'Validate before writing', code: '@score.setter\ndef score(self, value):\n    if not 0 <= value <= 100:\n        raise ValueError("score")\n    _____', prompt: 'Which final line safely updates the internal value?', choices: ['self._score = value', 'self.score = value', 'value = self._score'], answer: 0, explanation: 'Writing _score avoids recursively calling the property setter again.' },

    { id: 'inheritance-predict', topic: 'inheritance', kind: 'Predict output', title: 'Inherited shipping label', code: 'parcel = ExpressDelivery("PKG-204", "CDO", 2)\nprint(parcel.label())', prompt: 'ExpressDelivery does not redefine label(). Where is it found?', choices: ['Delivery, the base class', 'Nowhere; inherited methods cannot run', 'The abc module'], answer: 0, explanation: 'Attribute lookup checks ExpressDelivery first, then continues to Delivery and finds label().' },
    { id: 'inheritance-diagnose', topic: 'inheritance', kind: 'Diagnose', title: 'Initialize the delivery base', code: 'class ExpressDelivery(Delivery):\n    def __init__(self, tracking_id, destination, priority):\n        self.priority = priority', prompt: 'Why might tracking_id and destination be missing?', choices: ['Delivery.__init__ was never called through super()', 'Subclasses cannot store destinations', 'priority must be a class attribute'], answer: 0, explanation: 'super().__init__(tracking_id, destination) lets Delivery initialize the state it owns.' },
    { id: 'inheritance-complete', topic: 'inheritance', kind: 'Complete code', title: 'Reuse delivery initialization', code: 'class ExpressDelivery(Delivery):\n    def __init__(self, tracking_id, destination, priority):\n        _____\n        self.priority = priority', prompt: 'Choose the single-inheritance initialization call.', choices: ['super().__init__(tracking_id, destination)', 'self = Delivery(tracking_id)', 'Delivery = super(destination)'], answer: 0, explanation: 'super() follows the lookup order and calls Delivery.__init__ with the current object.' },

    { id: 'abstraction-predict', topic: 'abstraction', kind: 'Predict behavior', title: 'An incomplete payment type', code: 'class BankTransfer(PaymentMethod):\n    pass\n\nmethod = BankTransfer("Savings")', prompt: 'PaymentMethod declares abstract pay(). What happens?', choices: ['Python raises TypeError when BankTransfer is instantiated', 'BankTransfer silently receives an empty pay method', 'ABC charges the account automatically'], answer: 0, explanation: 'A payment subclass remains abstract until it implements every required abstract method.' },
    { id: 'abstraction-diagnose', topic: 'abstraction', kind: 'Diagnose', title: 'What the payment contract promises', code: 'class PaymentMethod(ABC):\n    @abstractmethod\n    def pay(self, amount):\n        pass', prompt: 'What does this declaration establish?', choices: ['A pay contract for concrete payment classes', 'Complete privacy for every account', 'Automatic bank authorization'], answer: 0, explanation: 'The abstract method names the operation callers may rely on while payment classes decide the details.' },
    { id: 'abstraction-complete', topic: 'abstraction', kind: 'Complete code', title: 'Mark the payment contract', code: 'from abc import ABC, abstractmethod\n\nclass PaymentMethod(ABC):\n    _____\n    def pay(self, amount):\n        pass', prompt: 'Which line makes pay abstract?', choices: ['@abstractmethod', '@property.setter', '@classmethod'], answer: 0, explanation: '@abstractmethod keeps the class abstract until a concrete payment implementation is supplied.' },

    { id: 'polymorphism-predict', topic: 'polymorphism', kind: 'Predict output', title: 'One alert, two channels', code: 'channels = [EmailNotification("ana@example.com"), SmsNotification("0917")]\nfor channel in channels:\n    print(channel.send("Class starts at 9"))', prompt: 'Which implementations run?', choices: ['EmailNotification.send, then SmsNotification.send', 'EmailNotification.send twice', 'Only the first channel’s'], answer: 0, explanation: 'Each call is dispatched from the runtime class of the object currently bound to channel.' },
    { id: 'polymorphism-diagnose', topic: 'polymorphism', kind: 'Diagnose', title: 'Avoid a channel type switch', code: 'if type(channel) is EmailNotification:\n    ...\nelif type(channel) is SmsNotification:\n    ...', prompt: 'What is the object-oriented alternative?', choices: ['Give both classes send() and call channel.send(message)', 'Convert every channel to email', 'Store class names as strings'], answer: 0, explanation: 'A shared method lets new notification channels participate without expanding a central type-check chain.' },
    { id: 'polymorphism-complete', topic: 'polymorphism', kind: 'Complete code', title: 'Dispatch alerts in a loop', code: 'for channel in channels:\n    _____', prompt: 'Which line uses the common interface?', choices: ['print(channel.send(message))', 'print(type(channel))', 'print(EmailNotification.send())'], answer: 0, explanation: 'Calling through channel lets Python select the send implementation for that object.' },
  ].map(Object.freeze));

  function topic(id) { return topics.find((item) => item.id === id) || topics[0]; }
  function forTopic(id) { return Object.freeze(challenges.filter((item) => item.topic === topic(id).id)); }
  function get(id) { return challenges.find((item) => item.id === id) || null; }
  return Object.freeze({ CONTENT_VERSION, topics, challenges, topic, forTopic, get });
})();
