/* Deployed ITCC47 semester release. Change currentCheckpointId deliberately, then rebuild. */
const ITCC47_RELEASE_PROFILE = Object.freeze({
  schemaVersion: 2,
  profileId: 'itcc47-2026-2027-s1',
  profileVersion: 2,
  currentCheckpointId: 'm4-queue-deque',
});
